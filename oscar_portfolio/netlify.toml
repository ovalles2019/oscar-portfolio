# Used when Netlify "Base directory" is oscar_portfolio (legacy Flutter path).
# Builds the Next.js portfolio in the sibling folder ../motion-site.
[build]
  command = "cd ../motion-site && npm ci && npm run build"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
